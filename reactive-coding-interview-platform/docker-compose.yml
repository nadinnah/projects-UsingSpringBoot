#docker compose defines a stack of services
version: '3'
services:
  db:
    image: postgres:16.4-alpine
    container_name: pg-db
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Jun23135
    ports:
      - "5432:5432"

